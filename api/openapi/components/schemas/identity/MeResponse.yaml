type: object
additionalProperties: false
properties:
  profile:
    $ref: "./Profile.yaml"
  email:
    description: Read from the access token, not the profiles table.
    type: [string, "null"]
    example: user@example.com
  roles:
    $ref: "./Roles.yaml"
required: [profile, email, roles]
