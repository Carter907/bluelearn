type: object
additionalProperties: false
description: A prerequisite topic the submission says has no guide yet.
properties:
  id:
    $ref: "../common/Uuid.yaml"
  title:
    type: string
    example: Trigonometry
required: [id, title]
